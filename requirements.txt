# Server / cloud deployment dependencies.
#
# This mirrors the base [project.dependencies] in pyproject.toml. It exists so the
# Dash cloud builder uses an explicit dependency list instead of auto-scanning
# imports -- the import scan otherwise picks up the guarded `import webview` in
# desktop.py and resolves it to the unrelated PyPI package `webview`, which needs
# native GTK/WebKit and fails to build. Desktop-only deps (pywebview) and the
# `share` extra are intentionally omitted; they aren't needed on a server.
dash==2.18.0
dash-bootstrap-components==1.3.0
dash-bio==1.0.2
dash-draggable==0.1.2
dash-cytoscape>=1.0.2
dash-ag-grid==32.3.2
plotly>=5.19.0,<6.0.0
colorcet>=3.1.0
cmcrameri>=1.9
glasbey>=0.3.0,<0.4
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
scipy>=1.11,<1.14
statsmodels>=0.14.5,<0.15
anndata>=0.12,<0.13
muon==0.1.7
dask>=2024.1
fsspec>=2024.1
s3fs>=2024.1
gcsfs>=2024.1
aiohttp>=3.9
logomaker>=0.8,<0.9
pyfaidx==0.7.2
pyjaspar==3.0.0
aiobotocore==2.15.2
boto3==1.35.36
botocore==1.35.36
s3transfer==0.10.3
urllib3==1.26.14
tqdm==4.64.1
python-dotenv==1.0.0
click
gunicorn==21.2.0
