# GUANACO -- Plotly Cloud deployment requirements (flat, no extras syntax).
# = core (21) + [cloud] (6, remote .h5ad over HTTPS) + [server] (gunicorn).
# This mouse config streams a remote .h5ad from Zenodo, so [cloud] is required;
# it has no genome browser, so [tracks] is intentionally omitted.

# --- core ---
dash>=2.18,<3
dash-bootstrap-components>=1.3,<2
dash-ag-grid>=32,<33
dash-draggable>=0.1.2,<0.2
dash-cytoscape>=1.0.2,<2
plotly>=5.19,<6
matplotlib>=3.7
colorcet>=3.1
cmcrameri>=1.9
glasbey>=0.3,<0.4
Pillow>=9
pandas>=2.0
numpy>=1.24
scipy>=1.11
scikit-learn>=1.1
statsmodels>=0.14
networkx>=2.7
anndata>=0.12,<0.13
muon>=0.1.7,<0.2
h5py>=3.8
psutil>=5.9

# --- cloud (remote data loading: Zenodo HTTPS .h5ad / remote .zarr) ---
# zarr is pinned to v2: anndata allows >=2.18.7 and our stores are zarr_format=2.
# zarr v3 made remote HTTP reads aiohttp-async-only, which deadlocks/raises
# ("Timeout context manager should be used inside a task") when a backed/lazy
# read runs inside a synchronous Dash callback under gunicorn. zarr v2 reads
# HTTP synchronously via fsspec's own sync loop, so backed .zarr works at request
# time. Keep <3 until the zarr-v3 + fsspec event-loop interaction is fixed.
zarr>=2.18.7,<3
fsspec>=2024.1
xarray>=2024.1
dask>=2024.1
s3fs>=2024.1
gcsfs>=2024.1
aiohttp>=3.9

# --- server (production WSGI) ---
gunicorn>=21,<23
