# Server / cloud deployment dependencies for Plotly Cloud.
#
# Upload the `src/` folder (this directory) as the app. It contains the `guanaco/`
# package, so all `from guanaco.* import ...` imports resolve locally and the builder
# never fetches `guanaco` from PyPI. Entrypoint module: guanaco.main
#
# This file MUST sit at the upload root (next to the guanaco/ folder). When present,
# the Plotly Cloud builder uses this explicit list instead of auto-scanning imports.
# The import scan otherwise mis-resolves two names to unrelated PyPI packages that
# then fail to build:
#   * `import guanaco`  (this app's own package) -> PyPI `guanaco` (CUDA tomography)
#   * `import webview`   (pywebview, desktop only) -> PyPI `webview` (needs GTK)
# Neither is listed here: `guanaco` is the app itself (uploaded as source, not
# installed from PyPI), and the desktop/share extras aren't needed on a server.
#
# Keep this in sync with [project.dependencies] in pyproject.toml.
dash==2.18.0
dash-bootstrap-components==1.3.0
dash-bio==1.0.2
dash-draggable==0.1.2
dash-cytoscape>=1.0.2
dash-ag-grid==32.3.2
plotly>=5.19.0,<6.0.0
colorcet>=3.1.0
cmcrameri>=1.9
glasbey>=0.3.0,<0.4
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
scipy>=1.11,<1.14
statsmodels>=0.14.5,<0.15
anndata>=0.12,<0.13
muon==0.1.7
dask>=2024.1
fsspec>=2024.1
s3fs>=2024.1
gcsfs>=2024.1
aiohttp>=3.9
logomaker>=0.8,<0.9
pyfaidx==0.7.2
pyjaspar==3.0.0
aiobotocore==2.15.2
boto3==1.35.36
botocore==1.35.36
s3transfer==0.10.3
urllib3==1.26.14
tqdm==4.64.1
python-dotenv==1.0.0
click
gunicorn==21.2.0
